---
interface Props {
  class?: string;
  style?: string;
}

const { class: className = "", style } = Astro.props;
---

<article
  class:list={[
    "rounded-lg bg-[#fcfcfc] border-0",
    "overflow-hidden relative group",
    className,
  ]}
  style={style}
>
  <slot />
</article>
